#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run linters
echo "📝 Linting Solidity files..."
npm run lint || {
  echo "❌ Solidity linting failed. Please fix errors before committing."
  exit 1
}

# Check formatting
echo "🎨 Checking code formatting..."
npm run format -- --check || {
  echo "⚠️  Code formatting issues detected. Running auto-format..."
  npm run format
  git add -A
  echo "✅ Code formatted automatically. Please review changes."
}

# Run tests
echo "🧪 Running tests..."
npm test || {
  echo "❌ Tests failed. Please fix failing tests before committing."
  exit 1
}

echo "✅ All pre-commit checks passed!"
